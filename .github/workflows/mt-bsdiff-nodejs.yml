name: mt-bsdiff-nodejs CI

on:
  push:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - name: install
      run: npm install
    - name: build
      run: npm run deploy
